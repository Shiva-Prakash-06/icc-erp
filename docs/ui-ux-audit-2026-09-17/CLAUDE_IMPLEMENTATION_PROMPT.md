# Prompt for Claude

Read `docs/ui-ux-audit-2026-09-17/AUDIT.md` and implement the UI/UX fixes in this repository. Treat `design-system/icc-erp/MASTER.md` and its page overrides as the visual source of truth, and use the audit screenshots only as evidence of current defects. Fix every P0 and P1 item first, then complete the screen-specific P2 upgrades. Preserve all Flask/Jinja routes, authorization rules, form actions and payload names, CSRF/version fields, server validation, exports, and no-JavaScript behavior. Do not invent data or change workflow meaning to make the UI easier.

Work through the implementation end to end: update shared primitives before page-specific CSS, verify both IGP and ICC fixtures, test the faculty/admin and scoped roles, and run the relevant typecheck, build, Playwright accessibility/responsive, workflow, and visual tests. Add regression tests for the home-route 429 defect, KPI drill-down mismatch, status contradictions, mobile action clipping, and dark-tile contrast. Do not stop at a plan. Finish the code, report the files changed and tests run, and update the audit with completion evidence or a precise reason for any deferred finding.
