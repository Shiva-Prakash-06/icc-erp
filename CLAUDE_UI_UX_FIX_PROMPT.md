# Claude implementation prompt

Read `docs/UI_UX_COMPUTER_USE_AUDIT.md` completely, then implement the entire audit in this repository. Treat the design system in `design-system/icc-erp/MASTER.md` and its page overrides as authoritative. Fix the shared P1 root causes first, then all P2 items, then P3 polish; do not stop at cosmetic tweaks or a subset of screens. Preserve every route, RBAC decision, form name/payload, CSRF behavior, canonical data value, backend workflow, report/API contract, and server-rendered fallback. Do not invent business functionality or fake data. Reuse accessible shared primitives, keep local Lucide assets, and avoid one-off patches. Verify all coverage-matrix screens at desktop, tablet, and 390/375 px, plus keyboard use, 200% zoom, reduced motion, validation/pending/empty/populated states, and the existing test suites. Eliminate all overlaps, clipping, hidden actions, unmapped icons, oversized empty cards, micro-text, and mobile table failures. As you work, mark audit items complete with the changed file(s) and verification evidence; do not claim completion while any P1/P2 item or regression remains.
