{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "PYTHONPATH= APP_ENV=production MIGRATION_ONLY=true DEMONSTRATOR=true uv run --frozen python -m flask --app run.py db upgrade && PYTHONPATH= APP_ENV=production MIGRATION_ONLY=true DEMONSTRATOR=true ACCEPTANCE_SEED=1 uv run --frozen python -m flask --app run.py seed-acceptance",
  "outputDirectory": ".",
  "functions": {
    "api/index.py": {
      "excludeFiles": "{.agents/**,docs/**,e2e/**,frontend/**,migrations/**,scripts/**,terraform/**,tests/**,**/googleapiclient/discovery_cache/documents/**,*.md,*.pdf,*.jpeg,compose.yaml,Dockerfile,Makefile,playwright.config.ts,requirements-dev.txt}"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
